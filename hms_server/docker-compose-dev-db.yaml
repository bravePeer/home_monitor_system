services:
  db:
    image: timescale/timescaledb:latest-pg18
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - 3306:5432
    volumes:
      - "hms_database_dev:/home/postgres/pgdata/data"

  adminer:
    image: adminer
    ports:
      - 3307:8080

volumes:
  hms_database_dev: